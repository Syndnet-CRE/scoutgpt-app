# Mapbox - get your token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Backend API URL
# Leave blank for local dev (Vite proxies /api → localhost:4000)
# Set in production, e.g.: https://scoutgpt-app.onrender.com/api
VITE_API_URL=
